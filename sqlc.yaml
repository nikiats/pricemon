version: "2"
sql:
  - engine: postgresql
    schema: internal/web/repository/migrations
    queries: internal/web/repository/queries
    gen:
      go:
        out: internal/web/repository/db
        sql_package: pgx/v5
        overrides:
          - db_type: uuid
            go_type: github.com/google/uuid.UUID
  - engine: postgresql
    schema: internal/pricemon/repository/migrations
    queries: internal/pricemon/repository/queries
    gen:
      go:
        out: internal/pricemon/repository/db
        sql_package: pgx/v5
        overrides:
          - db_type: uuid
            go_type: github.com/google/uuid.UUID
